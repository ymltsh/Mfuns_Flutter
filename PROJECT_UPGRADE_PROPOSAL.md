# Mfuns Flutter 项目进度与升级方案书

> 评估日期：2026-08-10  
> 评估依据：当前仓库代码、`README.md`、`MFUNS_FLUTTER_PLAN.md`、现有测试与 Android 构建配置。  
> 结论口径：**已实现**表示已有对应代码；**已验收**须通过真实 API、真机和发布链路验证，两者不能混用。

## 1. 管理摘要

项目已完成一个可演示、可覆盖核心内容消费闭环的 Flutter 客户端：用户可以浏览推荐/榜单/分区/时间线与关注动态，搜索文章和视频，查看图文或视频详情及用户主页，登录并恢复会话，观看多清晰度、多分 P 视频，读取及发布评论和弹幕，并进行点赞、点踩与收藏。

当前版本适合作为 **V1.0 功能完成版 / 内测候选版**，不建议直接定义为公开发布版。原因并非主功能缺失，而是接口契约、自动化测试、稳定性、安全合规、签名与发布工程尚未形成可重复的验收闭环。当前仓库尚无提交历史，所有文件处于首次暂存状态，也需要先建立可追溯的版本基线。

## 2. 当前完成度

| 维度 | 代码实现状态 | 验收状态 | 说明 |
| --- | --- | --- | --- |
| 内容浏览 | 已实现 | 时间线已按 HAR 对齐，其他待真实接口回归 | 推荐、热榜、分区、时间线、关注流、详情、相关推荐均有页面与 Repository 调用。 |
| 搜索 | 已实现 | 待真实接口回归 | 综合、文章、视频三类；用户搜索和分页尚未实现。 |
| 视频播放 | 已实现 | 待真机媒体回归 | 基于 `video_player`，含分 P、清晰度、进度、音量、倍速、全屏及普通弹幕。 |
| 图文详情 | 已实现 | 待内容兼容性回归 | 当前将正文转为纯文本展示，不等同于完整富文本渲染。 |
| 登录与会话 | 已实现 | 待失效/异常回归 | 表单登录、Secure Storage 保存 token、启动校验、退出本地会话均已具备。 |
| 评论与互动 | 已实现 | 待写操作回归 | 评论/回复读取和发送、点赞/点踩、收藏已有调用；需验证权限、失败恢复与幂等语义。 |
| 用户主页 | 已实现（只读） | 待真实页面回归 | 动态作者头像可进入用户资料页，展示资料、动态、文章、视频；关注写操作尚未接入。 |
| 个人中心 | 部分实现 | 待真实账号回归 | 历史、收藏夹及其内容已接入；投稿、消息通知、设置均明确为占位功能。 |
| 工程质量与发布 | 未完成 | 未完成 | 仅有 1 个基础 Widget 测试；无 CI、签名、监控、隐私材料或发布配置。 |

## 3. 已完成功能清单

### 内容与发现

- 首页推荐、热榜、分区树与分区内容。
- 时间线与登录后关注动态流、下拉刷新、分页、基础空态/错误态；时间线已以网页 `/timeline` 抓包中的 `/v1/feeds/new_reply_list` 为准。
- 综合、文章、视频搜索。
- 视频/文章详情、标签、相关推荐与链接复制。

### 播放与弹幕

- 视频地址获取、多清晰度切换、分 P 选择。
- 播放/暂停、进度拖动、音量、倍速、长按倍速、全屏与横竖屏切换。
- 普通弹幕读取、显示与登录后发送。

### 账号与社区

- 账号密码登录、token 安全存储、启动时会话恢复、失效时本地清理、手动退出。
- 历史记录、收藏夹与收藏内容浏览。
- 评论列表、回复展开、登录后发布评论和回复。
- 点赞、点踩、收藏状态读取与收藏写入。

### 基础技术能力

- 统一 API Client，默认注入移动端 UA，支持构建参数覆盖。
- 社区 token 直接置于 `Authorization`，未混用公开 API key。
- Android 网络权限、Android/iOS 工程骨架、Material 3 移动端三栏界面。

## 4. 欠缺与风险清单

### P0：发布前必须补齐

| 缺口 | 证据/影响 | 建议处理与验收标准 |
| --- | --- | --- |
| 核心链路没有自动化保障 | `test/` 仅含一个导航渲染测试；没有网络、DTO、Repository、播放器或写操作测试。 | 为 API Client、解析器、认证、分页、写操作及核心页面建立单元/Widget/契约测试；将成功、401/403、限流、空数据与格式变更纳入用例。 |
| 接口契约尚未冻结 | 多数调用按社区文档编码，尚缺脱敏响应样本和持续回归。 | 输出 `docs/api-contract.md` 与脱敏 fixtures；逐接口确认方法、参数、分页、鉴权、错误码及媒体头；禁止提交账号、密码和 token。 |
| 质量检查未形成结论 | 本次 `flutter analyze`、`flutter test` 均在 60 秒内无输出并超时，不能作为通过证明。 | 排查 Flutter/Gradle 工具链或依赖解析后，保存可复现的 analyze、test、debug/release 构建结果；以 CI 作为唯一合格口径。 |
| 发布安全未准备 | Android `release` 当前使用 debug 签名，且 applicationId 仍为模板命名。 | 确认正式包名，使用不入库的 keystore/CI 密钥完成 release 签名；校验 APK/AAB 可安装、升级和回滚。 |
| 隐私、合规与用户告知缺失 | App 保存会话 token，并会发送评论、弹幕、互动等写操作。 | 补齐隐私政策、第三方 SDK/数据收集清单、账号与内容规则、用户协议入口和写操作确认/错误提示；取得平台接口与媒体使用授权。 |

### P1：内测稳定性与体验升级

| 缺口 | 当前表现 | 升级方向 |
| --- | --- | --- |
| 分页/增量加载 | 时间线采用 `page/size` 分页，关注流采用 `start_id` 游标；搜索、分区、评论等仍固定首屏页码。 | 统一其余页面的游标/页码分页状态、去重、加载更多、刷新、可恢复滚动位置。 |
| 关注能力 | 已可读取登录用户的关注动态并进入作者主页；作者页“关注”按钮仍禁用。 | 先抓取并确认关注/取消关注接口、鉴权和状态字段，再接入写操作与失败恢复。 |
| 个人中心 | 投稿、消息通知、设置均为占位提示。 | 优先消息/通知与播放设置；投稿管理须单独确认接口权限、审核与风控。 |
| 图文渲染 | 详情正文被转为纯文本，且“展开”按钮无行为。 | 采用安全的富文本渲染方案，支持图片、链接、代码/引用块；为外链和图片加载设白名单与降级。 |
| 收藏管理 | 可新增收藏，页面侧重浏览；创建/编辑收藏夹及内容移除闭环不足。 | 补创建、编辑、删除收藏夹与移除内容，并提供确认、撤销和失败恢复。 |
| 网络韧性 | 使用原生 `HttpClient`，目前无受控重试、取消、缓存、请求追踪或统一超时策略。 | 引入分层网络策略：GET 限次重试、写操作防重复提交、取消与超时、缓存失效、脱敏日志和统一错误码映射。 |
| 可用性 | 已有基础空/错态与刷新，但缺骨架屏、无障碍、离线提示和深链。 | 建立加载态规范、Semantics/字号适配、网络状态提示、Deep Link 与分享回流。 |

### P2：规模化与差异化能力

- 站内信、通知中心、@我、互动消息与未读数。
- 用户主页、关注/粉丝、内容筛选、搜索历史与个性化推荐。
- 本地历史、搜索记录和可失效的图片/元数据缓存；离线下载仅在媒体授权和平台规则明确后评估。
- 投稿、草稿、审核状态、直播和社区管理等高风险写操作，均需单独立项。
- 崩溃监控、性能埋点、漏斗分析、灰度发布、远程配置与客服反馈闭环。

## 5. 建议目标架构

保留现有 Flutter 单仓库和功能分层思路，将当前以 `AppController + HomeRepository` 为中心的 MVP 结构逐步演进为可测试、可扩展架构：

```text
UI / 路由
  └─ Feature Controller（页面状态、分页、错误恢复）
       └─ Use Case（业务规则、权限与写操作编排）
            └─ Repository（领域接口）
                 ├─ Remote Data Source（DTO、API 契约）
                 └─ Local Data Source（缓存、历史、偏好）
```

- 网络：统一超时、认证、幂等、错误映射与脱敏日志；仅对幂等 GET 重试。
- 状态：先按 feature 拆分状态，避免单一 `AppController` 持续膨胀；可在重构窗口引入 Riverpod。
- 数据：DTO 与页面/领域模型隔离；为所有已接入接口保留版本化 fixture。
- 存储：会话仅进 Secure Storage；普通缓存使用可清理的数据库/文件缓存，设大小和失效上限。
- 质量：lint、格式化、测试、Android 构建、依赖审计、签名检查进入 CI 门禁。

## 6. 升级实施计划

### 阶段 A：冻结 V1.0 内测基线

1. 建立首个 Git 基线、版本号规范、变更日志和分支策略。
2. 恢复并记录 Flutter `analyze`、`test`、Android debug/release 构建结果。
3. 在授权测试账号与真机上走通登录、浏览、播放、评论、弹幕、互动、退出；形成冒烟清单与问题台账。
4. 固化 API 合约、脱敏 fixtures 与媒体兼容矩阵（协议、清晰度、分 P、网络切换、后台恢复）。

**阶段出口：** 每个核心接口有脱敏样本；核心用户路径有可重复的真机验收记录；所有质量门禁可在干净环境运行。

### 阶段 B：补齐发布阻塞项

1. 增加网络/解析/认证/Repository 单元测试与关键页面 Widget 测试。
2. 实现分页、统一错误恢复、加载占位、写操作去重和可诊断日志。
3. 完成正式包名、图标、启动页、签名、隐私材料、崩溃监控与 CI。
4. 完成内测渠道安装、升级、权限与弱网/断网验证。

**阶段出口：** CI 全绿；Release AAB/APK 使用正式签名；P0 风险无阻塞项；内测问题可定位、可回滚。

### 阶段 C：体验与社区增强

1. 分页和缓存优先，其次补富文本、收藏管理和播放设置。
2. 补齐作者页的关注/取消关注与消息通知，所有写接口先走灰度与回归。
3. 以崩溃率、播放成功率、接口失败率、登录成功率、核心路径完成率衡量版本效果。

**阶段出口：** 用户可完成“发现内容—消费内容—互动—沉淀收藏/历史—回访”的稳定闭环，并具备数据驱动的优化依据。

## 7. 验收指标与发布门槛

| 项目 | 发布门槛 |
| --- | --- |
| 构建 | `flutter analyze`、`flutter test`、Android release 构建在 CI 成功；无阻塞级 lint/测试失败。 |
| 核心路径 | 登录、会话恢复、首页、搜索、详情、播放、评论、弹幕、收藏、退出均有真机冒烟记录。 |
| 接口 | 已接入接口具备文档、脱敏 fixture、异常码处理和回归用例。 |
| 稳定性 | 弱网、无网、401/403、服务端异常、媒体地址失效、切后台和旋转屏均有明确降级。 |
| 安全合规 | 不记录明文账号/密码/token；release 正式签名；隐私与授权材料完整。 |
| 可观测性 | 崩溃、播放器失败、核心接口错误和关键漏斗可监测，日志不含敏感信息。 |

## 8. 优先级结论

下一步不应继续横向堆叠业务页面，而应优先完成 **“接口契约 + 测试/CI + 真机回归 + 签名/合规”** 四项发布基座。它们完成后，再按“分页与稳定性 → 个人中心完善 → 关注与消息 → 投稿等高风险能力”的顺序推进，可避免功能越多、回归成本越高的问题。
